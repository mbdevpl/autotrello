"""Initialization of unit tests of autotrello."""

import logging

logging.basicConfig(level=logging.DEBUG)
logging.getLogger().setLevel(logging.WARNING)
logging.getLogger('autotrello').setLevel(logging.DEBUG)
